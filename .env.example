# Deployed API — must be a full URL with host, e.g. https://finask.onrender.com/api/v1
# Do NOT set /api/v1 or your Vercel URL here: production would call the static site and return 404.
# On Vercel → Project → Settings → Environment Variables: use the Render URL below or leave unset (code defaults to Render).
VITE_API_URL=https://finask.onrender.com/api/v1

# Local backend (uncomment if your dev server uses this instead)
# VITE_API_URL=http://localhost:3000/api/v1

VITE_USE_MOCK=false

# Auth uses httpOnly cookies from the API. If SPA and API are on different origins,
# set JWT_COOKIE_SAMESITE=none on the backend (HTTPS) and list this app URL in FRONTEND_URL.
